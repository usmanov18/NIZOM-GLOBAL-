import 'package:flutter/material.dart';

class AdminAgentsScreen extends StatelessWidget {
  const AdminAgentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin Agents')),
      body: const Center(child: Text('Admin Agents Placeholder')),
    );
  }
}
