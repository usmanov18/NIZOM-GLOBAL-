import 'package:flutter/material.dart';

class DiscountPolicyScreen extends StatelessWidget {
  const DiscountPolicyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Discount Policy')),
      body: const Center(child: Text('Discount Policy Placeholder')),
    );
  }
}
