import 'package:flutter/material.dart';

class AdminPortfolioAssignmentScreen extends StatelessWidget {
  const AdminPortfolioAssignmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Portfolio Assignment')),
      body: const Center(child: Text('Portfolio Assignment Placeholder')),
    );
  }
}
