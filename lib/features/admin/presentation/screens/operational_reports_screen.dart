import 'package:flutter/material.dart';

class OperationalReportsScreen extends StatelessWidget {
  const OperationalReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Operational Reports')),
      body: const Center(child: Text('Operational Reports Placeholder')),
    );
  }
}
