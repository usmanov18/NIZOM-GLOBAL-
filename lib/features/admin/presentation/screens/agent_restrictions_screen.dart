import 'package:flutter/material.dart';

class AgentRestrictionsScreen extends StatelessWidget {
  const AgentRestrictionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Agent Restrictions')),
      body: const Center(child: Text('Agent Restrictions Placeholder')),
    );
  }
}
